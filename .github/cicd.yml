name: my_first_github_actions

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

    - name: Install dependencies
      run: 
        - sudo apt install python
        - pip install -U pytest

    - name: Run PyTest
      run: python -m pytest hello1.py